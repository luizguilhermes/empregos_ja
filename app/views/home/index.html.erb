<section>
	<%@jobs.each do |job|%>
	<div>
		<%= link_to job.title, job %>
		<h1><%= job.title %></h1> 
		<p><%= job.company %></p> 
		<p><%= job.location %></p>
		<p>
			<% if job.featured? %>
				<strong><%= 'Destaque' %></strong>
			<% end %>
		</p>
	</div>
	<% end %>
</section>